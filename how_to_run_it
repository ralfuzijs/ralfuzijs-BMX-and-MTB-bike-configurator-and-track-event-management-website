1. **Navigate to the Vue project directory**:
   Open a terminal in Visual Studio Code and navigate to the vue-project directory:
   ```sh
   cd vue-project
   ```

2. **Install dependencies**:
   Ensure all required dependencies are installed by running:
   ```sh
   npm install
   ```

3. **Start the development server**:
   Run the following command to start the development server:
   ```sh
   npm run dev
   ```

4. **Access the application**:
   After the server starts, it will provide a local development URL (e.g., `http://localhost:5173`). Open this URL in your browser to view the application.
